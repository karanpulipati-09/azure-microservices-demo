# Networking for AKS with Azure CNI (example minimal config). For advanced vnet/subnet, expand here.
# Placeholder: AKS will create required subnet if not provided when using managed vnet or provide subnet id for advanced setups.
